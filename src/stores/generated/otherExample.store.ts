// Auto-generated by generate-store - DO NOT EDIT
// Source: otherExample.ts

import { create } from 'zustand'
import { persist } from 'zustand/middleware'

interface UserState {
  id: string
  name: string
  email: string
  isActive: boolean
  age: number
  addresses: { street: string; city: string; state: string; zipCode: string; country: string; isDefault: boolean }[]
  orders: { orderId: string; status: "pending" | "processing" | "shipped" | "delivered" | "cancelled"; priority: "low" | "medium" | "high"; totalAmount: number; itemCount: number; product: { id: string; name: string; description: string; price: number; inStock: boolean; quantity: number } }[]
}

interface UserActions {
  setUser: (state: Partial<UserState>) => void
  resetUser: () => void
  setId: (value: UserState['id']) => void
  setName: (value: UserState['name']) => void
  setEmail: (value: UserState['email']) => void
  setIsActive: (value: UserState['isActive']) => void
  setAge: (value: UserState['age']) => void
  setAddresses: (value: UserState['addresses']) => void
  prependAddresses: (item: { street: string; city: string; state: string; zipCode: string; country: string; isDefault: boolean }) => void
  appendAddresses: (item: { street: string; city: string; state: string; zipCode: string; country: string; isDefault: boolean }) => void
  insertAddressesAt: (index: number, item: { street: string; city: string; state: string; zipCode: string; country: string; isDefault: boolean }) => void
  removeAddressesAt: (index: number) => void
  removeAddresses: (predicate: (item: { street: string; city: string; state: string; zipCode: string; country: string; isDefault: boolean }, index: number) => boolean) => void
  moveAddresses: (fromIndex: number, toIndex: number) => void
  updateAddressesAt: (index: number, item: Partial<{ street: string; city: string; state: string; zipCode: string; country: string; isDefault: boolean }> | ((item: { street: string; city: string; state: string; zipCode: string; country: string; isDefault: boolean }) => { street: string; city: string; state: string; zipCode: string; country: string; isDefault: boolean })) => void
  clearAddresses: () => void
  setOrders: (value: UserState['orders']) => void
  prependOrders: (item: { orderId: string; status: "pending" | "processing" | "shipped" | "delivered" | "cancelled"; priority: "low" | "medium" | "high"; totalAmount: number; itemCount: number; product: { id: string; name: string; description: string; price: number; inStock: boolean; quantity: number } }) => void
  appendOrders: (item: { orderId: string; status: "pending" | "processing" | "shipped" | "delivered" | "cancelled"; priority: "low" | "medium" | "high"; totalAmount: number; itemCount: number; product: { id: string; name: string; description: string; price: number; inStock: boolean; quantity: number } }) => void
  insertOrdersAt: (index: number, item: { orderId: string; status: "pending" | "processing" | "shipped" | "delivered" | "cancelled"; priority: "low" | "medium" | "high"; totalAmount: number; itemCount: number; product: { id: string; name: string; description: string; price: number; inStock: boolean; quantity: number } }) => void
  removeOrdersAt: (index: number) => void
  removeOrders: (predicate: (item: { orderId: string; status: "pending" | "processing" | "shipped" | "delivered" | "cancelled"; priority: "low" | "medium" | "high"; totalAmount: number; itemCount: number; product: { id: string; name: string; description: string; price: number; inStock: boolean; quantity: number } }, index: number) => boolean) => void
  moveOrders: (fromIndex: number, toIndex: number) => void
  updateOrdersAt: (index: number, item: Partial<{ orderId: string; status: "pending" | "processing" | "shipped" | "delivered" | "cancelled"; priority: "low" | "medium" | "high"; totalAmount: number; itemCount: number; product: { id: string; name: string; description: string; price: number; inStock: boolean; quantity: number } }> | ((item: { orderId: string; status: "pending" | "processing" | "shipped" | "delivered" | "cancelled"; priority: "low" | "medium" | "high"; totalAmount: number; itemCount: number; product: { id: string; name: string; description: string; price: number; inStock: boolean; quantity: number } }) => { orderId: string; status: "pending" | "processing" | "shipped" | "delivered" | "cancelled"; priority: "low" | "medium" | "high"; totalAmount: number; itemCount: number; product: { id: string; name: string; description: string; price: number; inStock: boolean; quantity: number } })) => void
  clearOrders: () => void
}

const defaultUserState: UserState = {
  id: '',
  name: '',
  email: '',
  isActive: false,
  age: 0,
  addresses: [],
  orders: [],
}

export const useUserStore = create<UserState & UserActions>()(
  persist(
    (set) => ({
  ...defaultUserState,
  setUser: (state) => set(state),
  resetUser: () => set(defaultUserState),
    setId: (value) => set({ id: value }),
    setName: (value) => set({ name: value }),
    setEmail: (value) => set({ email: value }),
    setIsActive: (value) => set({ isActive: value }),
    setAge: (value) => set({ age: value }),
    setAddresses: (value) => set({ addresses: value }),
    prependAddresses: (item) => set((state) => ({ addresses: [item, ...state.addresses] })),
    appendAddresses: (item) => set((state) => ({ addresses: [...state.addresses, item] })),
    insertAddressesAt: (index, item) => set((state) => {
      const newArray = [...state.addresses]
      newArray.splice(index, 0, item)
      return { addresses: newArray }
    }),
    removeAddressesAt: (index) => set((state) => ({
      addresses: state.addresses.filter((_, i) => i !== index)
    })),
    removeAddresses: (predicate) => set((state) => ({
      addresses: state.addresses.filter((item, index) => !predicate(item, index))
    })),
    moveAddresses: (fromIndex, toIndex) => set((state) => {
      const newArray = [...state.addresses]
      const [item] = newArray.splice(fromIndex, 1)
      newArray.splice(toIndex, 0, item)
      return { addresses: newArray }
    }),
    updateAddressesAt: (index, update) => set((state) => {
      const newArray = [...state.addresses]
      const currentItem = newArray[index]
      newArray[index] = typeof update === 'function'
        ? update(currentItem)
        : { ...currentItem, ...update }
      return { addresses: newArray }
    }),
    clearAddresses: () => set({ addresses: [] }),
    setOrders: (value) => set({ orders: value }),
    prependOrders: (item) => set((state) => ({ orders: [item, ...state.orders] })),
    appendOrders: (item) => set((state) => ({ orders: [...state.orders, item] })),
    insertOrdersAt: (index, item) => set((state) => {
      const newArray = [...state.orders]
      newArray.splice(index, 0, item)
      return { orders: newArray }
    }),
    removeOrdersAt: (index) => set((state) => ({
      orders: state.orders.filter((_, i) => i !== index)
    })),
    removeOrders: (predicate) => set((state) => ({
      orders: state.orders.filter((item, index) => !predicate(item, index))
    })),
    moveOrders: (fromIndex, toIndex) => set((state) => {
      const newArray = [...state.orders]
      const [item] = newArray.splice(fromIndex, 1)
      newArray.splice(toIndex, 0, item)
      return { orders: newArray }
    }),
    updateOrdersAt: (index, update) => set((state) => {
      const newArray = [...state.orders]
      const currentItem = newArray[index]
      newArray[index] = typeof update === 'function'
        ? update(currentItem)
        : { ...currentItem, ...update }
      return { orders: newArray }
    }),
    clearOrders: () => set({ orders: [] }),
}),
    {
      name: 'user-storage',
    }
  )
)
